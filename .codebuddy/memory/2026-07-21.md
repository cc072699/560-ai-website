# 2026-07-21

## 用户咨询：手动 Git 部署 560-ai-website 到内网服务器

### 上下文
- 用户项目：Next.js 16.2.10 + React 19，无数据库（JSON 存储）
- 目标服务器：Debian 12，IP 192.168.75.30，内网
- 用户已通过 SSH 连上服务器
- 远程仓库：https://github.com/cc072699/560-ai-website.git
- 本地 Node v25.8.1（服务器不能用 25，需装 20 LTS）

### 已交付
- 完整部署方案（含 5 个阶段：环境准备 → 项目改造 → 拉取构建 → PM2 守护 → 可选 Nginx）
- 保存到 `docs/DEPLOYMENT.md` 供后续查阅
- 11 项必改/注意事项清单
- 更新脚本 `deploy.sh` 模板

### 关键决策点（用户在本地需做）
1. 配置 `NEXTAUTH_SECRET` 环境变量（必配，否则 /admin 登录失效）
2. 上线前清空 `data/*_submissions.json` 测试数据
3. PM2 跑单实例（JSON 文件不支持并发写）
4. 修改 `next.config.ts` 的 `allowedDevOrigins` 加入服务器 IP
5. 服务器装 Node 20 LTS，不要用 25

### 用户偏好/事实
- 用户使用 GitHub 作为远端仓库（不是 cnb）
- 仓库地址：https://github.com/cc072699/560-ai-website.git
- 用户希望手动部署，不用 Vercel 等托管平台
